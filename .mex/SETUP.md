# Control MEX setup

Codex integration uses the official MEX Inbox and Relay skills. This repository's JavaScript WebUI can be navigated with MEX Code Graph; Java server code requires direct inspection. Use `ROUTER.md` for relevant context. No Hub is required for local CLI use.
